#include <bits/stdc++.h>
using namespace std;

typedef pair<int, int> pii;

static bool OOB(int x, int y, int Mx, int My)
{
	return (x < 0 || y < 0 || Mx <= x || My <= y);
}

int main() {
	return 0;
}
